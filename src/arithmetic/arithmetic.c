#include "../s21_decimal.h"

int s21_add(s21_decimal value_1, s21_decimal value_2, s21_decimal *result) {
    int status = OK;

    // if (!is_valid_decimal(value_1) || !is_valid_decimal(value_2)) {
    //     status = 
    // }

    return status;
}